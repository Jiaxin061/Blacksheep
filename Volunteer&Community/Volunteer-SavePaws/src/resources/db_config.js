// File: src/resources/db_config.js
// Configuration for XAMPP MySQL Database Connection

module.exports = {
    host: 'localhost',
    user: 'root',      // Default XAMPP user
    password: '',      // Default XAMPP password (empty)
    database: 'savepaws' // REPLACE with your actual database name if different
};
